# Module 05 — Agenda

- **SQLAlchemy Core & ORM** (`sqlalchemy/`) — DB-API, Engine, Core schema, ORM models, sessions, CRUD, select/filter/aggregate, relationships, joins, many-to-many
- **Alembic** (`alembic_ex/`) — models, `--autogenerate`, batch mode on SQLite, naming convention, full command reference
- **Same ORM on Postgres** (`postgres/`) — only the URL changes
