# Module 06 — Agenda

- **cnn-website** (`cnn-website/`) — Flask + SQLAlchemy 2.0 + Alembic + Postgres news reader on NewsAPI
  - Minimal MVT: three routes, two tables
  - Upsert on `articles.url`, one query file
  - Migrations via Alembic, Postgres in Docker Compose
