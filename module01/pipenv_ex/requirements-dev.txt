# pip has no concept of dependency groups, so the split between runtime and
# development tooling has to be two files kept in step by hand. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.4.2
bandit==1.9.2
ruff==0.14.14
pre-commit==4.6.1
