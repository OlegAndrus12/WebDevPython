# Module 01 — Agenda

- **Dependency management**
  - pip & venv (`pipenv_ex/`)
  - Poetry (`poetry_ex/`)
  - uv (`uv_ex/`)
  - Poetry vs. uv speed (`poetry_vs_uv_compare/`)
  - Logging (`poetry_logger/`)
  - Dev tooling: pytest, ruff, bandit, mypy, pre-commit (`tools/`)
- **Type annotations** (`type_annotation_extras/`) — hints vs. runtime checks, generics, `Protocol`, `TypedDict`, narrowing
- **Design principles & patterns**
  - SOLID, all five (`solid/`)
  - Patterns: Adapter, Observer, Proxy, Singleton, State, Template Method (`patterns/`)
