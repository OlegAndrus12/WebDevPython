# Module 02 — Agenda

- **Images and the Dockerfile**
  - Running someone else's image (`welcome-to-docker/`)
  - The minimal Dockerfile (`python-script/`)
  - Same shape, another language (`ruby-script/`)
  - Shipping a pre-built artifact (`java/`)
- **Container networking** (`flask-node/`) — why a user-defined network is needed
- **Docker Compose**
  - Compose as declared `docker run` (`flask-node/`)
  - Compose at scale, seven services (`microservices/`)
  - Compose with no build at all (`postgres_ex/`)
- **Packaging a real project**
  - uv in an image (`statusboard-uv/`)
  - The same app packaged with Poetry (`statusboard-poetry/`)
